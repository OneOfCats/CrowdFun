$("#projects-block").append('<%= escape_javascript( render partial: "projects/vertical_block", collection: @projects ) %>');
<% if @projects.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @projects %>');
<% else %>
  $('.pagination').html("");
<% end %>